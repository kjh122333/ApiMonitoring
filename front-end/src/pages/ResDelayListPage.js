import React from 'react';
import ResDelayListContainer from 'containers/resDelay/ResDelayListContainer';

const ResponseListPage = () => {
    window.scrollTo(0,0);
    return (
            <ResDelayListContainer />
    );
};

export default ResponseListPage;