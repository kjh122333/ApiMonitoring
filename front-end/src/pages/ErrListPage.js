import React from 'react';
import ErrorListContainer from 'containers/error/ErrorListContainer';

const ErrListPage = () => {
    window.scrollTo(0,0);
    return (
            <ErrorListContainer />
    );
};

export default ErrListPage;